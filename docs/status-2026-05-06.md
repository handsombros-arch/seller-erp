# seller-erp 현황 — 2026-05-06

## 한줄 요약

5/3 ~ 5/6 4일간 데이터 분석 페이지 디벨롭 + 광고/재고 인프라 정비 + 카테고리 비교/시계열/북마크/UI 개선 완료. production 안정화 + UX 통합 단계.

---

## 오늘 (5/6) 변경

| Commit | 내용 |
|---|---|
| `cad9f45` | 파서 '검색 결과' 헤더 인식 (쿠팡 페이지 라벨 변경 대응) |
| `75d8598` | Phase C 재고: 알림 배너 + algorithm.md 갱신 |
| `8d3fb3f` | Phase B 재고: 통합 조정 다이얼로그 + RG/자사 분리 표시 |
| `599c90d` | Phase A 재고: 음수 floor + refresh-sales 취소 제외 + alerts API |

## 5/3~5/5 누적 핵심 변경

### 데이터 분석 페이지 (`/data-analysis`)
- 카테고리 full-path dedup (leaf 이름 충돌 방지)
- competitor_snapshots 팀 공용 (user_id 필터 제거)
- 멀티 paste (`splitMultipleSnapshots` — '카테고리 결과' / '검색 결과' 헤더 둘 다)
- 실시간 검색 (debounce 150ms) + ancestor 자동 펼침
- 가변 depth UI (좌측 depth-color border)
- CTR 제외 % 정수 표시
- 컬럼 reorder (`@dnd-kit`, localStorage 영속화)
- 평균 판매가 fallback (winner_price → price_min/max midpoint)
- 카테고리 + 상품 북마크 (각각 노란/초록 별, localStorage)
- N개 카테고리 비교 다이얼로그 — 행 드래그 (top=기준점), 표/시계열 차트 두 탭
- 시계열 차트 (Recharts) — 카테고리당 라인, 6개 metric 토글
- 북마크 비교 버튼 (체크 안 해도 북마크된 카테고리 그대로 비교)
- 삭제 보호 (관리 모드 + 카테고리명 타이핑 확인)
- 상품명 클릭 → 쿠팡 검색 (첫 콤마 전 추출)
- Apple UI 톤 (`#0071E3` accent, rounded-[18px], -0.02em tracking)

### 광고 분석 페이지 (`/ad-analysis`)
- 빈 결과 시 데드락 방지 (날짜 자동 핏 + 진단 배너)
- API GET 페이지네이션 (Supabase 1000행 cap 회피)
- 업로드 청크 분할 + gzip (Vercel 4.5MB body 한도 회피)
- POST 이중 응답 fix (트리거+cron 충돌 → flag 우선)
- IDB 덮어쓰기 방지 (DB+IDB union)
- Storage 백업 silent 실패 가시화
- Apple 팔레트 일괄 치환 (Toss → Apple)

### 인프라 / 인증
- production Supabase 마이그레이션 누락 발견 + 수동 적용
  - `00050_ad_raw_rows_user_id.sql` (user_id 컬럼)
- Storage 'ad-files' 버킷 SQL (사용자 실행 필요)
- 헤더 배포 상태 배지 (`/api/deploy-info` + GitHub `/commits/main` 폴링, 진행률 % + 자동 새로고침)

### 재고 관리 (5/5~5/6 정비)
- **이중 차감 버그 fix** (트리거+applyOrders 충돌)
- **음수 재고 0 floor** — applyOrders / exchange 양쪽 통일 (트리거와 동일)
- **refresh-sales 취소 제외** — `cancelled_at IS NULL` 추가
- **창고 선택 일관성** — 양쪽 max-stock 우선
- **월별 실사 모드** (`__PHYSICAL_COUNT__:<date>` reason)
- **통합 조정 다이얼로그** — 단건/CSV/실사 3탭, 같은 다이얼로그에서 자유 전환
- **종합 현황 RG/자사 분리 표시** (`자사 N + RG M = X` breakdown)
- **알림 배너 3종** — 음수 재고 / 0 floor 시도 / 미매칭 SKU 주문

---

## 핵심 파일 인덱스

### 코드
- `src/app/(dashboard)/data-analysis/page.tsx` — 메인 카테고리 분석 페이지
- `src/app/(dashboard)/ad-analysis/page.tsx` — 광고 분석
- `src/app/(dashboard)/inventory/page.tsx` — 재고 관리 (5탭 + 통합 다이얼로그)
- `src/lib/coupang/parse-competitor-snapshot.ts` — 카테고리 텍스트 파서
- `src/lib/inventory/applyOrders.ts` — 주문→재고 차감/복구 코어
- `src/components/layout/header.tsx` — 헤더 (배포 상태 배지)
- `src/components/layout/deploy-status.tsx` — 배포 진행률 컴포넌트
- `src/app/api/inventory/{summary,alerts,exchange,history,import,route}` — 재고 API
- `src/app/api/inventory/apply-orders` — 백필 cron entry
- `src/app/api/cron/{daily,sync-orders,sync-rg-inventory,refresh-sales,snapshot-inventory}` — 자동화
- `src/app/api/rank-tracking/competitor/*` — 카테고리 스냅샷 CRUD + CSV/비교

### 마이그레이션 (`supabase/migrations/`)
- 총 54개. production 은 SQL Editor 수동 적용 (`schema_migrations` 테이블 없음 — 향후 정리 필요)
- `00017_order_driven_inventory.sql` — 핵심 트리거
- `00050_ad_raw_rows_user_id.sql` — 5/3 수동 적용
- `00052_competitor_snapshots.sql` / `00053_..._category.sql` / `00054_..._keyword_rank.sql`

### 문서 (`docs/`)
- `inventory-algorithm.md` — 재고 흐름 전체 (트리거 + applyOrders + 월별 실사 + 알림)
- `status-2026-05-06.md` — 이 파일

---

## 인프라

| 항목 | 상태 |
|---|---|
| Vercel | Production main 자동 배포. `cad9f45` 최신 |
| Supabase | production. `competitor_snapshots` 팀 공용. `ad_raw_rows` 5/3 user_id 수동 적용 |
| Vercel Cron | 매일 23:00 UTC (10:00 KST) `/api/cron/daily` — orders → rg → sales → snapshot |
| Fly.io Tokyo | 쿠팡 API 프록시 (Wing/RG) |
| Python 워커 (sourcing/) | RTX 5080 머신, Windows 작업 스케줄러 등록 |
| 배포 상태 배지 | 헤더에 표시. 새 commit 감지 시 주황 진행률, 완료 시 자동 새로고침 |

---

## 알려진 한계 / 후속 작업

| 항목 | 우선순위 | 설명 |
|---|---|---|
| Storage 'ad-files' 버킷 SQL | 중 | 사용자가 SQL Editor 에 실행 필요 (`docs/inventory-algorithm.md` 와 별도) |
| 4/19~5/2 광고 raw rows 손실분 | 낮 | 이중 silent 실패로 영구 손실. 쿠팡 PA 리포트 재다운로드 → 재업로드 시 자동 누적 |
| 마이그레이션 push 자동화 | 중 | production 이 수동 SQL 의존 — `supabase db push` 로 전환 권장 |
| applyOrders 다중 창고 분배 | 낮 | 현재 SKU 별 창고 1개 선택. 비례 분배 X |
| 교환 자동화 | 낮 | EXCHANGE 상태 들어와도 어느 SKU 로 교환인지 시스템이 모름 — 수동 |
| order_date vs shipping_date | 낮 | 실사 직전 in-flight 주문 누락 가능 — 실사 전 배송 cycle 정리 권장 |
| 새 검색 결과 포맷의 top100 필드 | 낮 | '검색 결과' 페이지에는 Top 100/Search%/Ad% 마커 없음 → NULL. 데이터 분석은 정상 |

---

## 메모리 인덱스 (`~/.claude/projects/.../memory/`)

- `project_seller_erp.md` — 전체 프로젝트 컨텍스트
- `feedback_seller_erp_category_dedup.md` — 카테고리 dedup 영구 규칙
- (이 외 Qoo10 관련 5개 + KC 인증 + Quote analyzer + 기타)

---

## 사용자 운영 흐름 (요약)

### 매일
- Vercel Cron 10:00 KST 자동 실행 → 주문 sync, 재고 차감, 매출 갱신, 스냅샷
- 사용자는 재고/주문 화면에서 결과만 확인

### 매주
- 데이터 분석 페이지에서 새 카테고리 (또는 키워드 검색) 결과 paste
- 북마크된 카테고리들 비교 + 시계열 차트로 추세 점검

### 매월
- 재고 페이지 → "월별 실사" 버튼 → CSV 업로드 → 자동 cutoff
- 그 이후 모든 채널 변동 자동 반영

### 광고 분석
- 쿠팡 PA 일별 키워드 리포트 다운로드 → ad-analysis 페이지에 드래그
- 자동 청크 분할 + gzip → DB 누적 (양쪽 PC 즉시 동기화)
